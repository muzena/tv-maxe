#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

build: build-stamp
	/usr/bin/msgfmt $(CURDIR)/channel-editor/po/hr.po --output-file $(CURDIR)/channel-editor/lng/hr/LC_MESSAGES/channeleditor.mo

build-stamp: 
	dh_testdir

clean:
	dh_testdir
	dh_testroot
	rm -rf build-stamp
	dh_clean
install: build
	dh_testdir
	dh_testroot
	dh_prep
	dh_installdirs
	# Add here commands to install the package into debian/tv-maxe
	install -p -dm 0755 $(CURDIR)/debian/channel-editor/usr/bin
	install -p -m 0755 $(CURDIR)/channeleditor $(CURDIR)/debian/channel-editor/usr/bin
	install -p -dm 0755 $(CURDIR)/debian/channel-editor/usr/share/applications
	install -p -m 0644 $(CURDIR)/tv-maxe-channel-editor.desktop $(CURDIR)/debian/channel-editor/usr/share/applications
	cp -r $(CURDIR)/channel-editor $(CURDIR)/debian/channel-editor/usr/share	

		
binary-indep: install

binary-arch: install
	dh_testdir
	dh_testroot
	dh_installdocs
	dh_link
	dh_strip
	dh_compress
	dh_fixperms
	dh_installdeb
	dh_gencontrol
	dh_md5sums
	dh_builddeb

binary: binary-indep binary-arch
